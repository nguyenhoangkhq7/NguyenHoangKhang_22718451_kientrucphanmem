FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Run tests
ENTRYPOINT ["pytest", "-v", "--cov=.", "--cov-report=term-missing"]
