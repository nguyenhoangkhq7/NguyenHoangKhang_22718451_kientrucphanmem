# Thiết lập mật khẩu để bảo vệ Redis (rất quan trọng khi deploy thực tế)
requirepass my_secret_redis_password

# Giới hạn bộ nhớ tối đa mà Redis được phép sử dụng
maxmemory 256mb

# Chính sách xử lý khi bộ nhớ đầy (xóa các key ít được sử dụng nhất)
maxmemory-policy allkeys-lru